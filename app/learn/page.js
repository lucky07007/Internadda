import Link from "next/link";import {lessons,roles,notes} from "../../data/content";
export default async function Learn({searchParams}){const sp=await searchParams;const selected=roles.find(r=>r.id===sp?.role)||roles[0];return <div className="page"><div className="container"><div className="page-head"><div className="eyebrow">Career paths</div><h1>Learn the skills you need to explain in an interview.</h1><p className="lead">Pick a role to see its interview-focused knowledge map, then use the shared interview foundations.</p></div><div className="role-tabs">{roles.map(r=><Link key={r.id} className={`role-tab ${r.id===selected.id?"active":""}`} href={`/learn?role=${r.id}`}>{r.title}</Link>)}</div><div className="card role-overview"><div><span className="tag">{selected.title}</span><h2>{selected.short}</h2><p className="lead">{selected.desc}</p></div><div className="skill-chips">{selected.skills.map(s=><span key={s}>{s}</span>)}</div></div><div className="section-inner"><div className="page-head"><div className="eyebrow">Your notes map</div><h2>{selected.title} essentials</h2></div><div className="grid grid3">{notes[selected.id].map(([t,b])=><div className="card" key={t}><span className="tag">Core note</span><h3>{t}</h3><p>{b}</p></div>)}</div></div><div className="section-inner"><div className="page-head"><div className="eyebrow">Shared interview foundations</div><h2>Learn these regardless of role.</h2></div><div className="list">{lessons.map((l,i)=><Link href={`/learn/${l.id}`} className="card lesson" key={l.id}><div className="lesson-left"><span className="number">{i+1}</span><div><span className="tag">{l.tag} · {l.mins} min</span><h3>{l.title}</h3><p>{l.body[0]}</p></div></div><span>→</span></Link>)}</div></div></div></div>}
