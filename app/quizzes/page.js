import QuizClient from "../../components/QuizClient";import {quizzes} from "../../data/content";export default function Quizzes(){return <div className="page"><div className="container"><div className="page-head"><div className="eyebrow">Quizzes</div><h1>Recall beats rereading.</h1><p className="lead">Each quiz gives an explanation after your answer. Use your score to decide which notes to revisit.</p></div><div className="grid grid2">{quizzes.map(q=><QuizClient key={q.id} quiz={q}/>)}</div></div></div>}
