import { useState } from "react"; import { Link,useNavigate } from "react-router-dom"; import { useAuth } from "../context/AuthContext";
export default function Signup(){const {signup,googleLogin}=useAuth();const [name,setName]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);const nav=useNavigate();const submit=async e=>{e.preventDefault();setBusy(true);setError('');try{await signup(email,password,name);nav('/start')}catch(err){setError(err.code==='auth/email-already-in-use'?'An account with this email already exists.':err.message)}finally{setBusy(false)}};return <div className="min-h-screen bg-slate-950 px-4 py-10 text-white"><div className="mx-auto max-w-md"><Link to="/" className="text-sm text-slate-400">← Back</Link><div className="mt-6 rounded-3xl border border-slate-800 bg-slate-900 p-7"><h1 className="text-2xl font-black">Create your learning account</h1><p className="mt-2 text-sm leading-6 text-slate-400">Your progress, roadmap and practice history stay connected to your account.</p><button onClick={async()=>{setBusy(true);try{await googleLogin();nav('/start')}catch(e){setError(e.message)}finally{setBusy(false)}}} className="mt-6 w-full rounded-xl bg-white py-3 font-bold text-slate-900">Continue with Google</button><div className="my-5 text-center text-xs text-slate-500">or use email</div><form onSubmit={submit} className="space-y-4"><input required value={name} onChange={e=>setName(e.target.value)} placeholder="Your full name" className="w-full rounded-xl border border-slate-700 bg-slate-950 px-4 py-3 outline-none"/><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email address" className="w-full rounded-xl border border-slate-700 bg-slate-950 px-4 py-3 outline-none"/><input required minLength={6} type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Create a password" className="w-full rounded-xl border border-slate-700 bg-slate-950 px-4 py-3 outline-none"/>{error&&<div className="rounded-xl bg-red-500/10 p-3 text-sm text-red-300">{error}</div>}<button disabled={busy} className="w-full rounded-xl bg-amber-400 py-3 font-black text-slate-950">{busy?'Creating…':'Create account'}</button></form><p className="mt-5 text-center text-sm text-slate-400">Already have an account? <Link to="/login" className="font-bold text-amber-300">Sign in</Link></p></div></div></div>}