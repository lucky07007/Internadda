import { useEffect } from "react"; import { useNavigate,useParams } from "react-router-dom"; export default function AssessmentGateway(){const nav=useNavigate();const {role}=useParams();useEffect(()=>{nav('/start',{replace:true,state:{role}});},[nav]);return <div className="min-h-screen grid place-items-center bg-slate-950 text-white">Opening your personalized learning journey…</div>}