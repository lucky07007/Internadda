import { useState } from "react"; import { Link,useNavigate,useLocation } from "react-router-dom"; import { FiArrowLeft,FiMail,FiLock } from "react-icons/fi"; import { useAuth } from "../context/AuthContext";
export default function Login(){const {login,googleLogin}=useAuth();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);const nav=useNavigate();const loc=useLocation();const submit=async e=>{e.preventDefault();setBusy(true);setError('');try{await login(email,password);nav(loc.state?.from||'/dashboard',{replace:true})}catch(err){setError(err.code==='auth/invalid-credential'?'Email or password is incorrect.':err.message)}finally{setBusy(false)}};return <div className="min-h-screen bg-slate-950 px-4 py-10 text-white"><div className="mx-auto max-w-md"><Link to="/" className="mb-8 inline-flex items-center gap-2 text-sm text-slate-400"><FiArrowLeft/> Back</Link><div className="rounded-3xl border border-slate-800 bg-slate-900 p-7 shadow-2xl"><div className="text-center"><img src="/logo.png" className="mx-auto h-12 w-12 rounded-2xl object-contain"/><h1 className="mt-4 text-2xl font-black">Welcome back</h1><p className="mt-2 text-sm text-slate-400">Continue your personalized learning.</p></div><button onClick={async()=>{setBusy(true);setError('');try{await googleLogin();nav('/dashboard')}catch(e){setError(e.message)}finally{setBusy(false)}}} disabled={busy} className="mt-7 w-full rounded-xl border border-slate-700 bg-white py-3 font-bold text-slate-900">Continue with Google</button><div className="my-5 flex items-center gap-3 text-xs text-slate-500"><span className="h-px flex-1 bg-slate-800"/>or<span className="h-px flex-1 bg-slate-800"/></div><form onSubmit={submit} className="space-y-4"><label className="block text-sm font-bold">Email<div className="mt-2 flex items-center rounded-xl border border-slate-700 bg-slate-950 px-3"><FiMail className="text-slate-500"/><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="w-full bg-transparent px-3 py-3 outline-none" placeholder="you@example.com"/></div></label><label className="block text-sm font-bold">Password<div className="mt-2 flex items-center rounded-xl border border-slate-700 bg-slate-950 px-3"><FiLock className="text-slate-500"/><input required type="password" value={password} onChange={e=>setPassword(e.target.value)} className="w-full bg-transparent px-3 py-3 outline-none" placeholder="Your password"/></div></label>{error&&<div className="rounded-xl border border-red-500/30 bg-red-500/10 p-3 text-sm text-red-300">{error}</div>}<button disabled={busy} className="w-full rounded-xl bg-amber-400 py-3 font-black text-slate-950 disabled:opacity-60">{busy?'Please wait…':'Sign in'}</button></form><p className="mt-6 text-center text-sm text-slate-400">New to Internadda? <Link to="/signup" className="font-bold text-amber-300">Create account</Link></p></div></div></div>}